node_modules
**/node_modules
**/dist
.git
infra
# The extension is a browser artifact, not a container. Its package.json is
# still copied in, because pnpm --frozen-lockfile validates the lockfile
# against every workspace importer and fails if one is missing.
apps/extension/src
apps/extension/public
apps/extension/test
